
OBJECTS = init.o miniz.o rzip.o

.PHONY: all clean

all: tools/unzip $(SHLIB)

tools/unzip.exe: miniz.c tools/unzip.c
	$(CC) $(CFLAGS) miniz.c tools/unzip.c -o tools/unzip

clean:
	rm -rf $(SHLIB) $(OBJECTS) \
	tools/unzip tools/unzip.exe tools/unzip.dSYM
